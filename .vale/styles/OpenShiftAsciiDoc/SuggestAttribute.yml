---
extends: substitution
level: suggestion
message: "Use the AsciiDoc attribute '%s' rather than the plain text product term '%s', unless your use case is an exception."
link: https://github.com/openshift/openshift-docs/blob/main/_attributes/common-attributes.adoc
action:
  name: replace
swap:
  OpenShift Container Platform: "{product-title}"
  Alibaba Cloud: "{alibaba}"
  Bare Metal Event Relay: "{redfish-operator}"
  cert-manager Operator for Red Hat OpenShift: "{cert-manager-operator}"
  distributed tracing data collection: "{OTELShortName}"
  distributed tracing platform: "{JaegerShortName}"
  distributed tracing: "{DTShortName}"
  Factory-precaching-cli tool: "{factory-prestaging-tool-caps}"
  factory-precaching-cli tool: "{factory-prestaging-tool}"
  "GitOps ZTP": "{ztp}"
  Hybrid Cloud Console: "{hybrid-console-second}"
  IBM Power: "{ibmpowerProductName}"
  IBM zSystems: "{ibmzProductName}"
  IBM LinuxONE: "{linuxoneProductName}"
  "Logging subsystem for Red Hat OpenShift": "{logging-title-uc}"
  "logging subsystem for Red Hat OpenShift": "{logging-title}"
  LVM Storage: "{lvms}"
  Migration Toolkit for Containers: "{mtc-full}"
  MTC: "{mtc-short}"
  odo: "{odo-title}"
  OpenShift Cluster Manager: "{cluster-manager}"
  OpenShift Data Foundation: "{rh-storage}"
  OpenShift image registry: "{product-registry}"
  OpenShift Kubernetes Engine: "{oke}"
  OpenShift Pipelines: "{pipelines-shortname}"
  OpenShift Platform Plus: "{opp}"
  OpenShift sandboxed containers: "{sandboxed-containers-first}"
  OpenShift Serverless Functions: "{FunctionsProductName}"
  OpenShift Serverless Operator: "{ServerlessOperatorName}"
  Pipelines as Code: "{pac}"
  Red Hat Hybrid Cloud Console: "{hybrid-console}"
  Red Hat OpenShift Cluster Manager: "{cluster-manager-first}"
  Red Hat OpenShift Data Foundation: "{rh-storage-first}"
  Red Hat OpenShift Dedicated: "{product-dedicated}"
  Red Hat OpenShift Dev Spaces: "{openshift-dev-spaces-productname}"
  (?<!Red Hat OpenShift )distributed tracing data collection: "{OTELName}"
  Red Hat OpenShift distributed tracing platform: "{JaegerName}"
  Red Hat OpenShift GitOps: "{gitops-title}"
  Red Hat OpenShift Local: "{openshift-local-productname}"
  Red Hat OpenShift Pipelines: "{pipelines-title}"
  Red Hat OpenShift Service Mesh: "{SMProductName}"
  Red Hat OpenShift support for Windows Containers: "{productwinc}"
  Red Hat Quay Container Security Operator: "{rhq-cso}"
  RHACM: "{rh-rhacm}"
  Run Once Duration Override Operator: "{run-once-operator}"
  Secondary Scheduler Operator: "{secondary-scheduler-operator}"
  Single-node OpenShift: "{sno-caps}"
  single-node OpenShift: "{sno}"
  TALM: "{cgu-operator}"
  Tekton Chains: "{tekton-chains}"
  Tekton Hub: "{tekton-hub}"
  Template Service Broker: "{tsb-name}"
  "Tempo Operator": "{TempoName}"
  Tempo(?! Operator): "{TempoShortName}"
  Three-node OpenShift: "{3no-caps}"
  three-node OpenShift: "{3no}"
  Topology Aware Lifecycle Manager: "{cgu-operator-full}"
  velero.io: "{velero-domain}"
  Web Terminal Operator: "{web-terminal-op}"