:navtitle: Comma-separated-values
:keywords: reference, rule, Comma-separated-values

= Comma-separated-values

Use `Comma-separated-values` rather than `%s`.

.Additional Resources

* link:{ssg-url}#comma-separated-values[]

